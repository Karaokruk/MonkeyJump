public enum Direction
{
    /************************************************************************/
    /* Directions LEFT and RIGHT must respectively be equal to -1 and 1     */
    /* in order to change a sprite orientation (by multiplying its X scale) */
    /************************************************************************/
    LEFT = -1,
    TOP = 0,
    RIGHT = 1,
    BOTTOM = 2
}
